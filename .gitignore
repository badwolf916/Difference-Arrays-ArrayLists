import java.util.ArrayList;

public class DifferenceArraysArrayLists {
    public static void main(String[] args) {
        // Demonstrating Arrays
        // Array is a fixed-size container to store elements of the same type
        int[] numArray = {1, 2, 3, 4, 5};  // Declaring and initializing an array with 5 elements
        System.out.println("Array Example:");
        
        // Accessing elements in the array using a for loop
        for (int i = 0; i < numArray.length; i++) {  // Arrays have a fixed length
            System.out.println("Element at index " + i + ": " + numArray[i]);
        }
        
        // Demonstrating ArrayList
        // ArrayList is a dynamic collection that can grow and shrink in size
        ArrayList<Integer> numArrayList = new ArrayList<>();  // Creating an ArrayList that holds Integer objects
        numArrayList.add(1);  // Dynamically adding elements to the ArrayList
        numArrayList.add(2);
        numArrayList.add(3);
        
        // Accessing elements in the ArrayList using a for loop
        System.out.println("\nArrayList Example:");
        for (int i = 0; i < numArrayList.size(); i++) {  // ArrayLists can change size, so we use size() to get the current size
            System.out.println("Element at index " + i + ": " + numArrayList.get(i));  // Using get() method to access elements
        }

        // Differences:
        // - Arrays are fixed in size, and elements can be accessed using indices.
        // - ArrayLists are dynamic, allowing the size to grow or shrink as needed.
        // - Arrays offer faster access to elements (fixed size), while ArrayLists offer more flexibility with built-in methods.
    }
}
